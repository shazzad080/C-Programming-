#include<stdio.h>

int main(){
    // char firstname[50];
    // scanf("%s",firstname);
    // printf("Your name is %s",firstname);
    // gets function
        char fullname[100];
      gets(fullname);
      puts(fullname);
    return 0;
}